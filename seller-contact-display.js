setInterval(()=>{let orders=[];try{orders=JSON.parse(localStorage.getItem('vslOrders')||'[]')}catch{}document.querySelectorAll('.seller-order-list .order-card').forEach(card=>{if(card.querySelector('.buyer-contact'))return;const transaction=card.querySelector('.order-head b')?.textContent,order=orders.find(item=>item.transaction===transaction);if(!order?.contact)return;const contact=document.createElement('p');contact.className='buyer-contact';contact.innerHTML='<b>Customer contact:</b> '+order.contact.name+' · '+order.contact.phone+'<br><b>Address:</b> '+order.contact.address;card.querySelector('.order-actions')?.before(contact)})},500);
